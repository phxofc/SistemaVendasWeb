package com.example.sistemavendas.repository;

import com.example.sistemavendas.models.Estado;
import org.springframework.data.jpa.repository.JpaRepository;

public interface EstadoRepositories extends JpaRepository<Estado, Long> {
}
